# v86b workflow intentionally disabled in this PR.
# The smoke remains available at python_lab/league_universe_smoke.py and is covered by package readiness parsing.
name: OmniBet v86b League Universe Disabled

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "v86b smoke is validated through package readiness in this PR"
