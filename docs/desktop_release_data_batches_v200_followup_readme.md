# README

The desktop package is a portable runtime package, not a source checkout. This follow-up makes the data batch commands work from the package root by bundling scripts, configs, Python helpers, and Rust CLIs.
