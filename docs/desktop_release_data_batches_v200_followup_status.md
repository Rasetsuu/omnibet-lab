# Status

This follow-up is required because the first archive-quoting fix merged before the package content fix was complete.

The release archive needs to include the data batch operational files, otherwise users extracting the package cannot run the generated batch scripts from the package directory.
