# v521-v530 Notes

This phase improves the normal Matches screen.

The result area now shows a readable card first and keeps raw data collapsed.

The old bottom debug area is hidden in normal mode.

The forward project plan is stored in:

```text
docs/roadmap_after_market_builder_v521_v530.md
```
