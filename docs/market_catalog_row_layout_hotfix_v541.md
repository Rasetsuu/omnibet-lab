# Market Catalog Row Layout Hotfix

This follow-up cleans the normal Matches result layout after the broader football catalog landed.

It changes glued text rows into structured rows with label, value, and status fields.

Example target shape:

```text
Label        Value                         Status
Confidence   LOW preview                   Preview now
Total cards  Needs card/referee history    Locked: needs data
```
