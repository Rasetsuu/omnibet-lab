# v621 Rust Rows

This phase adds Rust row types for the adapter sample work.

Added:

```text
rust-core/src/adapter_normalizer_v621.rs
```

Exposed through:

```text
rust-core/src/lib.rs
```

The module includes fixture rows, result rows, event rows, a pack struct, helper constructors, validation, and unit tests.
