# v531-v540 Catalog

This phase expands the normal match screen from a tiny output into a broad football catalog.

Rows are marked as preview, placeholder, or locked until data exists.

Current runtime is still only a fixture-level preview fallback.

Future engine work should add completed results, event data, player data, safe evaluation, and Rust runtime pieces.
