# CI Probe

This tiny file exists to test the ChatGPT -> GitHub branch/PR -> GitHub Actions -> log inspection loop.

If CI passes on this PR, future OmniBet work can happen in larger tested batches instead of requiring manual local testing after every small change.
